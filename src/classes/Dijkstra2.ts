import Node from './Node';

export default class Dijkstra {
  findShortestPath(graph: Node[][], startNode: Node, endNode: Node) {
    const visitedNodes: Node[] = [];
    startNode.distance = 0;
  }
}
